import { DisableOnSubmitDirective } from './disable-on-submit.directive';

describe('DisableOnSubmitDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableOnSubmitDirective();
    expect(directive).toBeTruthy();
  });
});
